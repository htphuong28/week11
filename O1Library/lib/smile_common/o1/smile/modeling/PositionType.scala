package o1.smile.modeling

/**
 * Enumerates the types of positions used in positioning and aligning elements within a graphical context.
 */
enum PositionType derives CanEqual:
  case Center, UpperLeftCorner
